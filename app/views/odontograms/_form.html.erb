<%= form_with(model: odontogram) do |form| %>
  <% if odontogram.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(odontogram.errors.count, "error") %> prohibited this odontogram from being saved:</h2>

      <ul>
        <% odontogram.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :patient_id, style: "display: block" %>
    <%= form.text_field :patient_id %>
  </div>

  <div>
    <%= form.label :exam_date, style: "display: block" %>
    <%= form.date_field :exam_date %>
  </div>

  <div>
    <%= form.label :exam_type, style: "display: block" %>
    <%= form.text_field :exam_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
